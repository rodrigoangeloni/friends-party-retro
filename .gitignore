# Dependencies
node_modules/

# Database (app/db/ only)
app/db/*.sqlite
app/db/*.sqlite-wal
app/db/*.sqlite-shm
app/db/*.sqlite-journal

# ROMs (strategic assets, not versioned)
app/public/roms/
!app/public/roms/.gitkeep

# Cores (EmulatorJS WASM, downloaded via npm)
app/public/data/cores/

# EmulatorJS assets (downloaded via npm package, not CDN)
app/public/data/
!app/public/data/.gitkeep

# Covers (uploaded via admin panel)
app/public/covers/
!app/public/covers/.gitkeep

# Uploads temp
app/tmp/

# Logs
*.log
pm2.logs/
logs/

# Entorno
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Build artifacts (if added later)
dist/
build/

# Test coverage (if added later)
coverage/
.nyc_output/
